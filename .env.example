# Set the real public domain before publishing. No invented domain is used.
NEXT_PUBLIC_SITE_URL=

# Netlify Forms accepts submissions on the site root when the hidden form
# template in public/__forms.html is detected during deployment.
# Replace with an API, webhook or CRM endpoint if the site moves away from Netlify.
NEXT_PUBLIC_ESTIMATE_FORM_ENDPOINT=/
